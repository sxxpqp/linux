apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql5.7-deployment
  labels:
    app: mysql
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql
  template:
    metadata:
      labels:
        app: mysql
    spec:
      containers:
        - name: mysql
          image: mysql:5.7.44
          ports:
            - containerPort: 3306
              name: mysql
          env:
            - name: MYSQL_ROOT_PASSWORD
              value: "123456"
            - name: MYSQL_ALLOW_EMPTY_PASSWORD
              value: "yes"
          volumeMounts:
            - name: mysql-data-volume
              mountPath: /var/lib/mysql
      volumes:
        - name: mysql-data-volume
          persistentVolumeClaim:
            claimName: mysql-pvc

---
apiVersion: v1
kind: Service
metadata:
  name: mysql-service
  labels:
    app: mysql
spec:
  type: NodePort # 可选：ClusterIP（集群内访问）、LoadBalancer（云服务商负载均衡）
  ports:
    - port: 3306
      targetPort: 3306
      nodePort: 30066 # 可选，NodePort范围默认30000-32767
  selector:
    app: mysql

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc
  labels:
    app: mysql
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi # 按需调整存储大小
  storageClassName: nfs-csi
  # 若使用默认StorageClass，无需指定storageClassName
  # storageClassName: standard

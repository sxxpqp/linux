services:
  gitlab-runner:
    image: gitlab/gitlab-runner:v18.4.0
    container_name: gitlab-runner
    restart: always
    volumes:
      - ./config:/etc/gitlab-runner
      - /var/run/docker.sock:/var/run/docker.sock
      - /srv/gitlab-runner/cache:/cache 